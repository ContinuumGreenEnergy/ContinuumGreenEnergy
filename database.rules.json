{
  "_comment": "FIX 7 — public read/write because there is NO Firebase Auth in this project. The 'login' is a hardcoded password in JS only (auth.js). FIX 2 — .indexOn entries are required so the orderByChild('ts') queries in realtime.listenDailyProgress / listenHse don't print runtime warnings and stay performant on the free tier. Deploy with: firebase deploy --only database",

  "rules": {
    ".read":  true,
    ".write": true,

    "dailyProgress": {
      ".indexOn": ["ts"]
    },

    "hse": {
      "observations": {
        ".indexOn": ["ts"]
      }
    }
  }
}
